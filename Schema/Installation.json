{
  "title": "Installation",
  "x-stoplight": {
    "id": "b1uf5qso41veg"
  },
  "type": "object",
  "description": "[extension of the publiccode standard] This object defines an installation of a given component within an environement",
  "properties": {
    "id": {
      "type": "string"
    },
    "organization": {
      "type": "string",
      "format": "uuid",
      "description": "The organization that installed the component"
    },
    "component": {
      "type": "string",
      "format": "uuid",
      "description": "The installed component"
    },
    "version": {
      "type": "string",
      "description": "The semantic version of the installation"
    },
    "environment": {
      "type": "string",
      "description": "The environement where the component is installed"
    }
  }
}